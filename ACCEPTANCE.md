# Google Earth 3D 级真实效果验收标准

本项目的“高保真”不再指程序化建筑外观，而是指由真实摄影测量 / 实景三维数据驱动的三维城市视觉结果。Google 官方将 Photorealistic 3D Tiles 定义为由高分辨率影像纹理覆盖的 3D mesh；本项目以这一视觉层级作为基线。

## PASS / FAIL

### A. 数据真实性（硬门槛）

- **PASS**：佛山目标区域加载的是摄影测量、倾斜摄影或同等级真实纹理 3D mesh，并包含真实地形起伏。
- **FAIL**：仅使用 OSM footprint 拉伸、随机高度、程序化窗户、通用材质或手工“类似”建筑来替代缺失实景数据。
- **FAIL**：目标地标只有图标或参数化示意模型，无法从实景网格中辨识。

### B. 视觉效果

- 近景可看到真实屋顶、立面纹理、道路、植被与地形共同构成的连续实景网格。
- 中远景不应突然切换成白模城市；允许 3D Tiles 正常 LOD 过渡。
- 相机支持自由俯仰、旋转、缩放以及地标飞行导航。
- “超清”模式的屏幕空间误差目标为 **8 px**；若设备显存不足可切换均衡/性能模式，但最终视觉验收使用超清模式。

### C. 工程与性能

- 城市数据按 OGC 3D Tiles 流式加载，不一次性下载整座佛山。
- 默认不离线缓存 Google Maps Platform 内容，也不抓取/提取 Google 数据生成二次数据集。
- 支持独立的自有 3D Tiles URL，以接入佛山本地授权倾斜摄影/实景三维数据。
- API Key 不提交到 GitHub；Google 模式只在当前浏览器 session 中保存凭据。
- 运行时展示动态数据来源 attribution；Google 模式同时展示 Google Maps 品牌标识。

### D. 最终验收判定

只有当**实际接入的数据源在佛山目标区域具有真实摄影测量/实景三维覆盖**时，才能判定“Google Earth 3D 级真实效果”通过。

代码具备渲染能力但数据源缺少佛山实景覆盖时，判定为：**软件能力 PASS / 最终视觉验收 FAIL**，不得用程序化建筑掩盖这一事实。
