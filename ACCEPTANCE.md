# 验收标准：佛山 Open Photorealistic Hybrid

本项目不再把“只有真实摄影测量 3D Tiles 才能运行”作为前置条件。默认路线采用开放数据构建高可信、视觉逼真的佛山三维城市，同时明确区分 **地理真实性**、**视觉真实性** 与 **摄影测量真实性**。

## A. 地理真实性（必须）

1. 建筑 footprint 必须来自公开真实地理数据，默认使用 Overture Maps Buildings PMTiles。
2. 建筑高度优先使用 Overture `height`；其次使用 `num_floors × 3.2m`；只有字段缺失时允许使用确定性的视觉估算。
3. UI 必须显示真实高度字段覆盖率，估算高度不得被表述为测量值。
4. 地形不得使用全城平面代替；默认使用公开 Terrain Tiles / Terrarium 高程。
5. 地标导航坐标使用 WGS84 真实经纬度。

## B. 视觉真实性（必须）

1. 普通建筑必须按建筑用途/属性使用分类型 PBR 立面，不允许全城统一灰盒。
2. 建筑立面应具备窗格、楼层节奏、不同粗糙度/金属度等视觉细节。
3. 必须支持真实卫星影像贴地；默认提供无 Key 的开放影像源，并允许切换天地图影像。
4. 相机由城市级视角进入街区时，应动态加载更高层级地形和建筑瓦片。
5. 数据加载失败必须明确提示，不得用随机建筑替换真实数据。

## C. 摄影测量真实性（增强项）

当未来获得佛山倾斜摄影、CIM 实景三维或授权 OGC 3D Tiles 时，可作为独立高保真层加入。只有这类数据存在时，才允许声明“逐像素/逐表面接近 Google Earth 摄影测量真实性”。

本开放数据默认模式的目标是：**城市级和航拍级视觉高度逼真，建筑位置与 footprint 真实，高度尽可能真实，缺失高度显式估算；不伪装成摄影测量原始网格。**

## D. 数据源要求

- Overture Maps 当前锁定发布：`2026-08-19.0`。
- 默认影像：EOX Sentinel-2 2016，CC BY 4.0。
- 可选影像：EOX 2024（非商业许可）或天地图影像（按开发授权执行）。
- 地形：Mapzen Terrain Tiles / Terrarium。
- 不使用 Overpass/OSM 建筑 extrusion 作为默认高保真数据源。

## E. 可执行检查

```bash
npm run check
```

必须全部退出码为 0。
